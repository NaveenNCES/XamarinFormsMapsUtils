<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:customrenderer="clr-namespace:MapsXamarinForms.CustomRenderer"
             x:Class="MapsXamarinForms.Views.MainPage"
             Title="{Binding Title}">
    <StackLayout>
        <Button Text="GeoJson" Command="{Binding GeoJsonCommand}" />
        <Button Text="Cluster" Command="{Binding ClusterCommand}" />
    </StackLayout>
</ContentPage>